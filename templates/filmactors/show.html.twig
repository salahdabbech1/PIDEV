{% extends 'base.html.twig' %}

{% block title %}Filmactors{% endblock %}

{% block body %}
    <h1>Filmactors</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filmactor.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('filmactors_index') }}">back to list</a>

    <a href="{{ path('filmactors_edit', {'id': filmactor.id}) }}">edit</a>

    {{ include('filmactors/_delete_form.html.twig') }}
{% endblock %}
