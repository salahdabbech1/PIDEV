
= <div id="qodef-side-area">
    <h1>Edit User</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.password) }}
    {{ form_row(form.email) }}
    <input style="visibility: hidden;" type="number" id="{{ form.role.vars.id }}" name="{{ form.role.vars.full_name }}" value="0">
    {% do form.role.setRendered %}


    {% do form.image.setRendered %}
    <div class="form-group col-md-6">


        <label for="{{ form.image.vars.id }}"><label for="{{ form.image.vars.id }}" class="required">Image</label></label>
        <div class="fileinput-new" data-provides="fileinput">
            <div class="fileinput-preview" data-trigger="fileinput" style="width: 200px; height:150px;"></div>
            <span class="btn btn-primary  btn-file">
												<span class="fileinput-new">Select</span>
												<span class="fileinput-exists">Change</span>
												<input type="file" id="{{ form.image.vars.id }}" name="{{ form.image.vars.full_name }}" value="">
											</span>
            <a href="#" class="btn btn-danger fileinput-exists" data-dismiss="fileinput">Remove</a>

        </div>
        {% do form.image.setRendered %}
    </div>
    <button class="btn">{{ button_label|default('update') }}</button>
    {{ form_end(form) }}
    <a href="{{ path('hom') }}">back to list</a>






    </tr>
    </td>


</div>

