<form method="post" action="{{ path('tactor_delete', {'id': tactor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tactor.id) }}">
    <button class="btn">Delete</button>
</form>
